{
  "name": "tradesentinel-workspace",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "dev": "pnpm --filter @tradesentinel/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "format:check": "pnpm -r format:check",
    "contracts:generate": "uv run python scripts/export_openapi.py packages/contracts/openapi.json && openapi-typescript packages/contracts/openapi.json -o packages/contracts/src/generated/openapi.ts",
    "contracts:check": "uv run python scripts/check_openapi.py"
  },
  "devDependencies": {
    "openapi-typescript": "^7.9.1"
  }
}

